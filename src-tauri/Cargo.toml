[package]
name = "papers"
version = "0.1.0"
description = "A universal personal workspace layered over Windows."
authors = ["Papers are Papers"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.5.6", features = [] }

[dependencies]
tauri = { version = "2.11.5", features = [] }
