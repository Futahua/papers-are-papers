{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The local Papers windows. Machine actions remain behind typed Rust commands.",
  "windows": ["main", "companion"],
  "permissions": ["core:default"]
}
